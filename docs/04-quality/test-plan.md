# 测试计划（Draft）

- Rust：迁移、CRUD、软删除清理、笔记本删除、路径穿越/符号链接、导入冲突、SSE 事件和取消。
- Vue：Pinia store、800ms 保存、搜索、主题/语言、FIM Tab/Escape 和 AI 流状态。
- E2E：首次启动、重启恢复、导入预览、删除恢复、模型配置和拖放。

在 Windows 开发机运行 `npm run test:unit`、`npm run build`、`cargo test`；macOS 安装包需 CI runner 证据。
