# 故事地图（Approved）

## MVP

- 启动应用 → 查看全部笔记 → 新建/编辑/自动保存 → 搜索、删除、恢复。
- 管理笔记本 → 创建、重命名、删除（笔记移到未分类）。
- 查看知识库 → 创建个人/本地库 → 新建文件夹、导入、重命名、删除到回收站 → 递归搜索和预览。
- 设置主题、语言和模型元数据；API Key 仅由 Rust 写入系统凭据存储。
- 编辑器 AI 操作以 Channel 事件流传输，支持取消；FIM 默认关闭。

## 范围外

RAG、知识库问答、Agent、日程、自动化、账号、云同步、备份、分享和 Friday 数据迁移。
