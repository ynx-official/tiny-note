# 架构概览

状态：Approved

Vue 3 负责页面与编辑器，Pinia 负责客户端状态；Tauri commands 是唯一的本地能力边界；Rust 模块负责 SQLite、路径校验、文件监听、回收站、模型请求和系统凭据；知识库文件以应用数据目录中的相对路径管理。

Agent 同样遵守该边界：Vue 只提交请求、呈现流式事件和收集审批决定；Rust Agent Runtime 负责模型工具调用循环、工具注册与参数校验、策略判断、执行、取消、用量记录和 SQLite 审计。写工具必须经过持久化审批；Skills 由 Rust 在应用数据目录维护，并按任务需要渐进加载。
